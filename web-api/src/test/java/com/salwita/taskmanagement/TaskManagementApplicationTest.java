package com.salwita.taskmanagement;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertNotNull;

class TaskManagementApplicationTest {

    @Test
    void applicationMainMethodExists() {
        // Simple test to verify the main method exists and can be called
        assertNotNull(TaskManagementApplication.class);
        // We can add more sophisticated tests later when we have entities
    }
}